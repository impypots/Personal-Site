import React from 'react';

function Contact(props) {
    return(
        <div id="contact" style={{
            display: props.display
        }}>
            <h1>This is the contact page, BATCH</h1>
        </div>
    )
}

export default Contact;